import { SettingsView } from '@/src/components/settings/SettingsView'

export const metadata = {
  title: '設定 | Studist デバイスサービス',
}

export default function SettingsPage() {
  return <SettingsView />
}
